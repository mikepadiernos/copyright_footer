name: Copyright Footer (canvas)
type: module
description: Canvas-compatible fork of Copyright Footer with strict configuration validation compatibility.
core_version_requirement: ^11 || ^12
dependencies:
  - drupal:block
